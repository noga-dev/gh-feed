import 'package:flutter/material.dart';

class TrendingRepos extends StatelessWidget {
  const TrendingRepos({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
